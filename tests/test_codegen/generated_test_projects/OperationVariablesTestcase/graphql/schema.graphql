input ConnectedInput {
  boolVar: Boolean!
}

interface Node {
  id: ID!
}

type Query {
  user(foo: Int! = 0): UserWithFriend!
}

type UserWithFriend implements Node {
  id: ID!
  name: String!
  friend(connectedArg: ConnectedInput!): UserWithFriend
}
