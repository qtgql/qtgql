type Mutation {
  changeFriendName(connected: Boolean!, newName: String!): UserWithFriend!
}

interface Node {
  id: ID!
}

type Query {
  user: UserWithFriend!
}

type UserWithFriend implements Node {
  id: ID!
  name: String!
  friend(connectedArg: Boolean!): UserWithFriend
}
