type Mutation {
  addTag(postId: ID!, tag: String!): Post!
}

interface Node {
  id: ID!
}

type Post implements Node {
  id: ID!
  content: String!
  tags: [String!]!
}

type Query {
  post: Post!
}
