interface Node {
  id: ID!
}

type Query {
  node(userId: ID!): Node!
  user: User!
}

type User implements UserInterface & Node {
  id: ID!
  name: String!
  age: Int!
}

interface UserInterface implements Node {
  id: ID!
  name: String!
  age: Int!
}
