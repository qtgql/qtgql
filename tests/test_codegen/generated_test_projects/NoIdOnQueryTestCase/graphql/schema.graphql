type Mutation {
  """randomizes the current const user"""
  randomizeConstUser: User!
}

interface Node {
  id: ID!
}

type Query {
  user: User!
  constUser: User!
}

scalar UUID

type User implements Node {
  id: ID!
  name: String!
  age: Int!
  agePoint: Float!
  male: Boolean!
  uuid: UUID!
}
